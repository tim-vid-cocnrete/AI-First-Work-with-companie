---
description: План интеграции с Sendsay API для автовыгрузки метрик.
category: Integrations
owner: Process_Lead
---
# План: Автовыгрузка Метрик Рассылок из Sendsay API

**Дата:** 23 января 2026
**Задача:** Настроить еженедельную автоматическую выгрузку статистики email-рассылок из Sendsay

---

## 📊 Что Будем Получать

| Метрика | API-поле | Описание |
|---------|----------|----------|
| Доставляемость | `issue.delivery_rate` | % успешно доставленных |
| Открываемость | `issue.open_rate` | % уникальных открытий |
| Кликабельность | `issue.click_rate` | % уникальных кликов от доставленных |
| CTOR | `issue.click_open_rate` | % кликов от открывших |
| Отписки | `issue.unsub_rate` | % отписок |
| **Абсолютные цифры:** |  |  |
| Доставлено | `issue.deliv_ok` | Количество |
| Уникальные открытия | `issue.u_readed` | Количество |
| Уникальные клики | `issue.u_clicked` | Количество |

---

## 🔧 Техническая Информация

### API Endpoint
```
POST https://api.sendsay.ru/general/api/v100/json/{ACCOUNT}
```
*Замените `{ACCOUNT}` на название вашего аккаунта в Sendsay.*

### Аутентификация
**Вариант 1 (Рекомендуется):** API-ключ в теле запроса
```json
{
  "action": "stat.uni",
  "apikey": "YOUR_API_KEY",
  ...
}
```

**Вариант 2:** API-ключ в заголовке
```
Authorization: sendsay apikey=YOUR_API_KEY
```

### Метод: `stat.uni` (Универсальная Статистика)
Этот метод позволяет выбирать любые поля и фильтровать по датам.

---

## 📝 Пошаговый План Реализации

### Шаг 1: Получить API-ключ
1. Войдите в аккаунт Sendsay
2. Перейдите в **Настройки → Сублогины** (или API)
3. Создайте новый API-ключ или используйте существующий
4. Сохраните ключ в безопасное место

### Шаг 2: Создать Python-скрипт

```python
import requests
import json
from datetime import datetime, timedelta

# --- НАСТРОЙКИ ---
SENDSAY_ACCOUNT = "ваш_аккаунт"  # Замените на ваш аккаунт
API_KEY = "ваш_api_key"           # Замените на ваш ключ
API_URL = f"https://api.sendsay.ru/general/api/v100/json/{SENDSAY_ACCOUNT}"

# --- РАСЧЁТ ДАТ ---
today = datetime.now()
week_ago = (today - timedelta(days=7)).strftime("%Y-%m-%d")

# --- ЗАПРОС ---
payload = {
    "action": "stat.uni",
    "apikey": API_KEY,
    "select": [
        "issue.id",
        "issue.dt",
        "issue.subject",
        "issue.delivery_rate",
        "issue.open_rate",
        "issue.click_rate",
        "issue.click_open_rate",
        "issue.unsub_rate",
        "issue.deliv_ok",
        "issue.u_readed",
        "issue.u_clicked"
    ],
    "filter": [
        {
            "a": "issue.dt",
            "op": ">=",
            "v": week_ago
        }
    ],
    "order": [
        {"a": "issue.dt", "order": "desc"}
    ]
}

response = requests.post(API_URL, json=payload)
data = response.json()

# --- ОБРАБОТКА ---
if "errors" in data:
    print(f"Ошибка: {data['errors']}")
else:
    print(f"Статистика за последние 7 дней ({week_ago} - {today.strftime('%Y-%m-%d')}):\n")
    for row in data.get("list", []):
        print(f"📧 {row.get('issue.subject', 'Без темы')}")
        print(f"   Дата: {row.get('issue.dt')}")
        print(f"   Доставлено: {row.get('issue.deliv_ok')}")
        print(f"   Open Rate: {row.get('issue.open_rate')}%")
        print(f"   Click Rate: {row.get('issue.click_rate')}%")
        print(f"   CTOR: {row.get('issue.click_open_rate')}%")
        print(f"   Отписки: {row.get('issue.unsub_rate')}%")
        print("-" * 40)
```

### Шаг 3: Настроить Планировщик (Cron / Task Scheduler)

**Windows (Task Scheduler):**
1. Откройте **Планировщик заданий** (Task Scheduler)
2. Создайте новую задачу
3. Триггер: **Еженедельно**, выберите день (например, Понедельник 9:00)
4. Действие: **Запустить программу**
   - Программа: `python`
   - Аргументы: `C:\путь\к\скрипту\sendsay_stats.py`

**Linux/Mac (Cron):**
```bash
# Открыть crontab
crontab -e

# Добавить строку (каждый понедельник в 9:00)
0 9 * * 1 /usr/bin/python3 /path/to/sendsay_stats.py >> /path/to/logs/sendsay.log 2>&1
```

### Шаг 4: Сохранение Данных (Опционально)

**Вариант A: Google Sheets**
Добавьте в скрипт интеграцию с Google Sheets API для автоматической записи данных.

**Вариант B: CSV-файл**
```python
import csv

with open(f"sendsay_stats_{today.strftime('%Y-%m-%d')}.csv", "w", newline="", encoding="utf-8") as f:
    writer = csv.DictWriter(f, fieldnames=[
        "issue.id", "issue.dt", "issue.subject", 
        "issue.open_rate", "issue.click_rate"
    ])
    writer.writeheader()
    writer.writerows(data.get("list", []))
```

**Вариант C: База данных (SQLite/PostgreSQL)**
Для накопления исторических данных.

---

## ⚠️ Важные Замечания

1. **API Rate Limits:** Sendsay может ограничивать количество запросов. Для еженедельной выгрузки это не проблема.

2. **Пагинация:** Если у вас много рассылок, используйте параметры `skip` и `first`:
   ```json
   "skip": 0,
   "first": 100
   ```

3. **Фильтрация по кампании:** Можно добавить фильтр по конкретной кампании:
   ```json
   {"a": "issue.campaign.id", "op": "=", "v": "123"}
   ```

4. **Безопасность:** Не храните API-ключ в открытом виде в коде. Используйте переменные окружения:
   ```python
   import os
   API_KEY = os.environ.get("SENDSAY_API_KEY")
   ```

---

## ✅ Чек-лист

- [ ] Получить API-ключ в Sendsay
- [ ] Создать Python-скрипт
- [ ] Протестировать запрос вручную
- [ ] Настроить сохранение (CSV/Sheets/DB)
- [ ] Настроить планировщик (Cron/Task Scheduler)
- [ ] Проверить первую автоматическую выгрузку

---

**Источник:** [Sendsay API Documentation](https://sendsay.ru/api/api.html) — раздел "Универсальная статистика"
